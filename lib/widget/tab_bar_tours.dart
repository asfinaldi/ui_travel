import 'package:flutter/material.dart';

class TabBarTours extends StatelessWidget {
  const TabBarTours({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Text('Ini Tours'),
    );
  }
}
